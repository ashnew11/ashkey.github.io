<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Website Keren</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    /* Reset dan font */
    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
    body { background: #0f0f0f; color: #fff; line-height: 1.6; }

    /* Navbar */
    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 50px;
      background: #111;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    nav a { color: #fff; text-decoration: none; margin-left: 20px; transition: 0.3s; }
    nav a:hover { color: #ff6b6b; }

    /* Hero Section */
    .hero {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #1e1e1e, #222);
      text-align: center;
    }
    .hero h1 { font-size: 4rem; margin-bottom: 20px; color: #ff6b6b; }
    .hero p { font-size: 1.2rem; color: #aaa; margin-bottom: 40px; max-width: 600px; }
    .hero button {
      padding: 15px 30px;
      font-size: 1rem;
      border: none;
      cursor: pointer;
      background: #ff6b6b;
      color: #fff;
      border-radius: 50px;
      transition: 0.3s;
    }
    .hero button:hover { background: #ff4757; }

    /* Cards Section */
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      padding: 100px 50px;
      background: #111;
    }
    .card {
      background: #1a1a1a;
      padding: 30px;
      border-radius: 15px;
      transition: 0.3s;
      text-align: center;
    }
    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 20px rgba(255,107,107,0.4);
    }
    .card h3 { margin-bottom: 15px; color: #ff6b6b; }
    .card p { color: #ccc; font-size: 0.95rem; }

    /* Footer */
    footer {
      text-align: center;
      padding: 50px 20px;
      background: #111;
      color: #777;
    }

    /* Animasi Scroll */
    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: all 1s ease-out;
    }
    .fade-in.show {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="logo"><h2>MyWeb</h2></div>
    <div class="menu">
      <a href="#hero">Home</a>
      <a href="#services">Services</a>
      <a href="#contact">Contact</a>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero" id="hero">
    <h1>Selamat Datang!</h1>
    <p>Kami membuat website yang keren, modern, dan interaktif dengan HTML, CSS, dan JavaScript.</p>
    <button onclick="alert('Selamat datang di website keren ini!')">Mulai Sekarang</button>
  </section>

  <!-- Cards Section -->
  <section class="cards" id="services">
    <div class="card fade-in">
      <h3>Desain Web</h3>
      <p>Membuat desain web modern, responsif, dan user-friendly.</p>
    </div>
    <div class="card fade-in">
      <h3>Animasi</h3>
      <p>Animasi interaktif untuk meningkatkan pengalaman pengguna.</p>
    </div>
    <div class="card fade-in">
      <h3>SEO & Marketing</h3>
      <p>Meningkatkan visibilitas website dan engagement pelanggan.</p>
    </div>
    <div class="card fade-in">
      <h3>Support</h3>
      <p>Dukungan teknis 24/7 untuk semua kebutuhan web Anda.</p>
    </div>
  </section>

  <!-- Footer -->
  <footer id="contact">
    <p>&copy; 2025 MyWeb. All rights reserved.</p>
  </footer>

  <!-- JavaScript Animasi Scroll -->
  <script>
    const faders = document.querySelectorAll('.fade-in');

    const appearOptions = {
      threshold: 0.2,
      rootMargin: "0px 0px -50px 0px"
    };

    const appearOnScroll = new IntersectionObserver(function(entries, appearOnScroll){
      entries.forEach(entry => {
        if(!entry.isIntersecting) return;
        entry.target.classList.add('show');
        appearOnScroll.unobserve(entry.target);
      });
    }, appearOptions);

    faders.forEach(fader => {
      appearOnScroll.observe(fader);
    });
  </script>
</body>
</html>
